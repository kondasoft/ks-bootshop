{
    "name": "ks-bootshop",
    "version": "2.0.0",
    "description": "Free Shopify Theme powered by Bootstrap v5",
    "scripts": {
        "scss": "sass --no-source-map scss/theme.scss:assets/theme.css && postcss assets/theme.css --replace --use autoprefixer && minify assets/theme.css > assets/theme.min.css",
        "watch": "npm-watch",
        "lint": "eslint ./"
    },
    "watch": {
        "scss": {
            "patterns": [
                "scss"
            ],
            "extensions": "scss"
        }
    },
    "dependencies": {
        "autoprefixer": "^10.2.4",
        "minify": "^7.0.0",
        "npm-watch": "^0.7.0",
        "postcss": "^8.2.4",
        "sass": "^1.32.6"
    },
    "devDependencies": {
        "eslint": "^7.19.0"
    }
}
